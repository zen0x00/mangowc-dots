#!/bin/sh

swayidle \
	timeout 60 'swaylock --grace 10 --fade-in 4' \
	timeout 80 'notify-send -u low -i display "Display Power" "Outputs are going OFF"' \
	resume 'notify-send -u low -i display "Display Power" "Outputs are now ON"' \
	before-sleep 'swaylock'
